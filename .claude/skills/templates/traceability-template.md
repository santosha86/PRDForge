---
bundle_path: "{{bundle_path}}"
delivery_path: "{{delivery_path}}"
generated_at: "{{generated_at}}"
verdict: "{{verdict}}"                   # PASS | FAIL
failed_gates: [{{failed_gates_csv}}]
retry_count: {{retry_count}}
counts:
  full: {{count_full}}
  partial: {{count_partial}}
  none: {{count_none}}
  total: {{count_total}}
skipped_clarifications: {{skipped_clarifications_count}}
---

# Traceability — {{prd_title}}

> **Coverage matrix produced by `@architecture-reviewer` (Stage 5 — V1 lightweight).**
> Validates that every numbered requirement in `(PRD ∪ CLARIFICATIONS)` has explicit coverage in the delivery at `{{delivery_path}}`. Fails closed on any `coverage_status: NONE`.

---

## Verdict

{{#if verdict_is_PASS}}
✅ **PASS** — every requirement has coverage in the delivery.
{{else}}
❌ **FAIL** — one or more requirements lack coverage. See the matrix below for specifics. Fix hints are listed at the end.
{{/if}}

| Status | Count |
|---|---|
| `FULL` | {{count_full}} |
| `PARTIAL` | {{count_partial}} |
| `NONE` | {{count_none}} |
| **Total** | **{{count_total}}** |

{{#if skipped_clarifications_count}}
⚠️ **{{skipped_clarifications_count}} clarifications were skipped at Stage 1.** They are surfaced as risk items in the fix-hints section regardless of overall verdict.
{{/if}}

---

## Coverage Matrix

| `R-*` | Title | Source | Status | Covered by | Notes |
|---|---|---|---|---|---|
{{#each traceability_matrix}}
| `{{requirement_id}}` | {{title}} | `{{source}}` | {{status_badge}} | {{covered_by_list}} | {{notes_or_dash}} |
{{/each}}

---

## Fix Hints

{{#if has_fix_hints}}
{{#each fix_hints}}
- {{this}}
{{/each}}
{{else}}
*No remediations required. Every requirement has at least PARTIAL coverage with notes.*
{{/if}}

---

## Re-run

To regenerate this matrix after changing the delivery:

```bash
prdforge trace --bundle {{bundle_path}} --delivery {{delivery_path}}
```

This file is replaceable — re-running overwrites it. History is in git.

---

*Generated by PRDForge — see [`07_PRDForge_PRD_V4.md`](../07_PRDForge_PRD_V4.md) for the spec, [`docs/BUNDLE_FORMAT.md`](../docs/BUNDLE_FORMAT.md) for the bundle contract.*
